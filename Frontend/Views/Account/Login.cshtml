@model Hms.Web.Models.Auth.PatientLoginViewModel
@{
    ViewData["Title"] = "Patient Login";
}

<<<<<<< HEAD
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-body p-4">
                    <h2 class="mb-3">Patient Login</h2>
                    <p class="text-muted">Request a login OTP using patient ID.</p>

                    <div class="alert alert-info">
                        For testing, the OTP is shown in the running Auth API console output.
                    </div>

                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success">@TempData["Success"]</div>
                    }

                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger">@TempData["Error"]</div>
                    }

                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <form asp-action="Login" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="PatientId" class="form-label"></label>
                            <input asp-for="PatientId" class="form-control form-control-lg" />
                            <span asp-validation-for="PatientId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="MobileNumber" class="form-label"></label>
                            <input asp-for="MobileNumber" class="form-control form-control-lg" />
                            <span asp-validation-for="MobileNumber" class="text-danger"></span>
                        </div>

                        @if (Model.OtpSent)
                        {
                            <div class="mb-3">
                                <label asp-for="OtpCode" class="form-label"></label>
                                <input asp-for="OtpCode" class="form-control form-control-lg" placeholder="Enter OTP here" />
                                <span asp-validation-for="OtpCode" class="text-danger"></span>
                            </div>

                            <button type="submit" name="submitButton" value="verifyOtp" class="btn btn-success btn-lg w-100">
                                Verify OTP & Login
                            </button>
                        }
                        else
                        {
                            <button type="submit" name="submitButton" value="sendOtp" class="btn btn-primary btn-lg w-100">
                                Send Login OTP
                            </button>
                        }
                    </form>

                    <div class="mt-3 text-center">
                        <a asp-action="ActivatePortal">New patient? Activate portal first</a>
=======
<div class="auth-shell">
    <div class="container py-4 py-lg-5">
        <div class="row justify-content-center">
            <div class="col-xl-10">
                <div class="card auth-card">
                    <div class="row g-0">
                        <div class="col-lg-5">
                            <div class="auth-aside">
                                <span class="badge-soft mb-3"><i class="bi bi-shield-lock"></i> Secure patient access</span>
                                <h1 class="h2 fw-bold mb-3">Patient sign in</h1>
                                <p class="mb-0 text-white-50">Use your patient ID and registered mobile number to receive a one-time password and access the portal.</p>

                                <ul class="step-list">
                                    <li>
                                        <span class="step-bullet">1</span>
                                        <div><strong>Enter details</strong><div class="text-white-50">Patient ID and mobile number must match the patient record.</div></div>
                                    </li>
                                    <li>
                                        <span class="step-bullet">2</span>
                                        <div><strong>Receive OTP</strong><div class="text-white-50">The OTP is generated by the Auth API and shown in its console for testing.</div></div>
                                    </li>
                                    <li>
                                        <span class="step-bullet">3</span>
                                        <div><strong>Verify and continue</strong><div class="text-white-50">Once verified, your session token is stored in MVC session state.</div></div>
                                    </li>
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-7">
                            <div class="auth-form-panel">
                                <div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-4">
                                    <div>
                                        <h2 class="mb-1">Login to HMS</h2>
                                        <p class="text-muted mb-0">OTP-based login for patients.</p>
                                    </div>
                                    <a asp-action="ActivatePortal" class="btn btn-outline-primary rounded-pill px-3">
                                        <i class="bi bi-person-plus me-1"></i> Activate portal
                                    </a>
                                </div>

                                <div class="info-strip mb-4">
                                    <div class="fw-semibold mb-1">Testing note</div>
                                    <div class="text-muted small">If you get connection errors here, start <strong>HealthcareGateway</strong>, <strong>Hms.AuthApi</strong>, and <strong>Hms.PatientsApi</strong> before opening this page.</div>
                                </div>

                                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                                <form asp-action="Login" method="post" class="row g-3">
                                    @Html.AntiForgeryToken()

                                    <div class="col-md-6">
                                        <label asp-for="PatientId" class="form-label"></label>
                                        <input asp-for="PatientId" class="form-control" placeholder="Enter patient ID" />
                                        <span asp-validation-for="PatientId" class="text-danger small"></span>
                                    </div>

                                    <div class="col-md-6">
                                        <label asp-for="MobileNumber" class="form-label"></label>
                                        <input asp-for="MobileNumber" class="form-control" placeholder="Enter registered mobile number" />
                                        <span asp-validation-for="MobileNumber" class="text-danger small"></span>
                                    </div>

                                    @if (Model.OtpSent)
                                    {
                                        <div class="col-12">
                                            <label asp-for="OtpCode" class="form-label"></label>
                                            <input asp-for="OtpCode" class="form-control" placeholder="Enter OTP" />
                                            <span asp-validation-for="OtpCode" class="text-danger small"></span>
                                        </div>

                                        <div class="col-12 d-grid d-sm-flex gap-2">
                                            <button type="submit" name="submitButton" value="verifyOtp" class="btn btn-success btn-lg flex-fill">
                                                <i class="bi bi-check2-circle me-1"></i> Verify OTP &amp; Login
                                            </button>
                                            <button type="submit" name="submitButton" value="sendOtp" class="btn btn-outline-primary btn-lg">
                                                Resend OTP
                                            </button>
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="col-12 d-grid">
                                            <button type="submit" name="submitButton" value="sendOtp" class="btn btn-primary btn-lg">
                                                <i class="bi bi-send me-1"></i> Send Login OTP
                                            </button>
                                        </div>
                                    }
                                </form>
                            </div>
                        </div>
>>>>>>> ee49ab9fb4705d2037d437f343847efd9ce49e85
                    </div>
                </div>
            </div>
        </div>
    </div>
<<<<<<< HEAD
</div>
=======
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
>>>>>>> ee49ab9fb4705d2037d437f343847efd9ce49e85
